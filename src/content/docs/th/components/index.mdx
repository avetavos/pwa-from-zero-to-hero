---
title: '.astro Components'
description: '.astro component คืออะไร เทียบกับ React component อย่างไร และโมดูลนี้ครอบคลุมเรื่องอะไรบ้าง'
---

import ReactAstro from '../../../../components/ReactAstro.tsx';
import AstroPlayground from '../../../../components/AstroPlayground.tsx';
import Diff from '../../../../components/Diff.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

ใน React คุณเขียน component เป็นฟังก์ชัน JavaScript ที่ return JSX ออกมา ส่วนใน Astro คุณเขียน component เป็นไฟล์ `.astro` ซึ่งเป็น superset ของ HTML ที่มีบล็อก JavaScript/TypeScript แบบ optional อยู่ด้านบนสุด เรียกว่า **frontmatter**

การปรับ mental model ที่สำคัญ: React function component ทำงานในเบราว์เซอร์ทุกครั้งที่ re-render แต่ Astro component ทำงาน **ครั้งเดียวตอน build time (หรือบนเซิร์ฟเวอร์)** แล้วส่งออกมาเป็น HTML ล้วน ๆ โดย default จะมี JavaScript เป็นศูนย์ ไม่มี virtual DOM ไม่มีการ re-render ไม่มีต้นทุนของ hydration

โมดูลนี้จะพาคุณไล่ดูทุกองค์ประกอบของ `.astro` component โดยยึดโยงกับสิ่งที่คุณรู้อยู่แล้วจาก React

## บทเรียนในโมดูลนี้

| บทเรียน | สิ่งที่คุณจะได้เรียนรู้ |
|---|---|
| [Frontmatter & Template](/astro-for-react-developers/th/components/frontmatter-template/) | บล็อก `---` เทียบกับ function body ของ React |
| [Props](/astro-for-react-developers/th/components/props/) | `Astro.props` เทียบกับ props ของ React |
| [Slots](/astro-for-react-developers/th/components/slots/) | `<slot />` เทียบกับ `children` |
| [Expressions](/astro-for-react-developers/th/components/expressions/) | template expression เทียบกับ JSX |
| [Importing Components](/astro-for-react-developers/th/components/importing-components/) | การประกอบ component ใน `.astro` |
| [Server-Only & Scripts](/astro-for-react-developers/th/components/server-only/) | JS เป็นศูนย์โดย default และวิธีเพิ่ม JS ฝั่งเบราว์เซอร์ |

## .astro component ตัวแรกของคุณ

React component และตัวเทียบเท่าในรูปแบบ `.astro` วางเทียบกัน:

export const indexReact = `// Greeting.jsx
export default function Greeting({ name }) {
  return <h1>Hello, {name}!</h1>;
}`;

export const indexAstro = `---
// Greeting.astro
const { name } = Astro.props;
---
<h1>Hello, {name}!</h1>`;

<ReactAstro client:visible react={indexReact} astro={indexAstro} />

และนี่คือแนวคิดเดียวกันในรูปแบบหน้าเว็บที่รันได้จริง:

export const indexCode = `---
const name = "React developer";
const year = new Date().getFullYear();
---
<html lang="en">
  <head><meta charset="utf-8" /><title>Hello</title></head>
  <body>
    <h1>Hello, {name}!</h1>
    <p>Built in {year}. No JavaScript shipped to the browser.</p>
  </body>
</html>`;

<AstroPlayground client:visible code={indexCode} />

<Diff title="React component vs .astro component">
- React: ฟังก์ชัน JS ที่ **return JSX** ทำงานในเบราว์เซอร์ทุกครั้งที่ render
- Astro: ไฟล์ `.astro` ที่มีบล็อก `---` frontmatter แบบ optional ทำงาน **ครั้งเดียวตอน build/server time** แล้วส่งออกมาเป็น HTML
- เหมือนกันทั้งคู่: รับ props ได้ รองรับการประกอบ (composition) และใช้ syntax `{expr}` แบบ JSX ใน template
- ความแตกต่างหลัก: `.astro` component ส่ง JavaScript ไปยังเบราว์เซอร์ **เป็นศูนย์** โดย default
</Diff>

export const quizIndex = [
  {
    q: "โค้ดใน frontmatter (---) ของ Astro component ทำงานเมื่อใด?",
    options: [
      "ในเบราว์เซอร์ทุกครั้งที่โหลดหน้า",
      "ครั้งเดียวตอน build time หรือบนเซิร์ฟเวอร์",
      "ภายใน Web Worker",
      "เฉพาะตอนที่ component mount เท่านั้น",
    ],
    answer: 1,
  },
  {
    q: ".astro component ธรรมดาส่ง JavaScript ไปยังเบราว์เซอร์เท่าไรโดย default?",
    options: ["เท่ากับ React", "runtime shim ขนาดเล็ก", "JavaScript เป็นศูนย์", "ขึ้นอยู่กับจำนวน props"],
    answer: 2,
  },
  {
    q: "template ของ .astro ใช้ syntax ใดในการฝัง expression?",
    options: ["`{{ expr }}`", "`<%= expr %>`", "`{expr}`", "`$expr`"],
    answer: 2,
  },
];

<Quiz client:visible id="components/index" questions={quizIndex} />

<ProgressTracker client:visible id="components/index" />
