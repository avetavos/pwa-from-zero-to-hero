---
title: 'Content & Data'
description: 'วิธีที่ Astro จัดการข้อมูล: frontmatter ทำงานตอน build/server, content collections สำหรับ MD/MDX เทียบกับ React data-fetching'
---

import ReactAstro from '../../../../components/ReactAstro.tsx';
import AstroPlayground from '../../../../components/AstroPlayground.tsx';
import Diff from '../../../../components/Diff.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

ใน React คุณดึงข้อมูลด้วย `getServerSideProps`, `getStaticProps` หรือ `useEffect+fetch` ใน Astro ข้อมูลอยู่ใน frontmatter `---` (ทำงานตอน build หรือ server) หรือใน content collections (MD/MDX ที่มี type safety) ไม่มีการ fetch ฝั่ง browser โดย default

## บทเรียนในโมดูลนี้

| บทเรียน | สิ่งที่จะได้เรียนรู้ |
|---|---|
| [Frontmatter Fetch](/astro-for-react-developers/th/content/frontmatter-fetch/) | `await fetch()` ใน frontmatter เทียบกับ `getServerSideProps` |
| [Content Collections](/astro-for-react-developers/th/content/content-collections/) | `defineCollection` + zod schema เทียบกับ contentlayer |
| [Rendering Markdown](/astro-for-react-developers/th/content/rendering-markdown/) | `render(entry)` เทียบกับ MDX ใน Next |
| [Dynamic from Data](/astro-for-react-developers/th/content/dynamic-from-data/) | `getStaticPaths()` จาก collection |

## การดึงข้อมูล: React vs Astro

export const fetchReact = `// pages/blog.tsx (Next.js)
import { useEffect, useState } from 'react';

export default function Blog() {
  const [posts, setPosts] = useState([]);

  useEffect(() => {
    fetch('https://api.example.com/posts')
      .then(r => r.json())
      .then(setPosts);
  }, []);

  return (
    <ul>
      {posts.map(post => (
        <li key={post.id}>{post.title}</li>
      ))}
    </ul>
  );
}`;

export const fetchAstro = `---
// src/pages/blog.astro
const res = await fetch('https://api.example.com/posts');
const posts = await res.json();
---
<ul>
  {posts.map((post) => (
    <li>{post.title}</li>
  ))}
</ul>`;

<ReactAstro client:visible react={fetchReact} astro={fetchAstro} />

สังเกต: ใน Astro `await fetch()` อยู่โดยตรงใน frontmatter มันทำงานตอน build time (static) หรือ per-request (SSR) ไม่มี `useEffect` ไม่มี loading state ไม่มี JavaScript ฝั่ง client ที่ถูกส่งไปสำหรับ fetch

## ตัวอย่าง inline data

export const inlineCode = `---
// In a real project, replace this array with await fetch(...) in the frontmatter
const posts = [
  { id: 1, title: "Getting started with Astro" },
  { id: 2, title: "Content collections deep dive" },
  { id: 3, title: "Islands architecture explained" },
];
---
<html lang="en">
  <head><meta charset="utf-8" /><title>Posts</title></head>
  <body style="font-family:sans-serif;padding:2rem">
    <h1>Blog Posts</h1>
    <ul>
      {posts.map((post) => (
        <li style="margin:0.5rem 0">{post.title}</li>
      ))}
    </ul>
  </body>
</html>`;

<AstroPlayground client:visible code={inlineCode} />

<Diff title="React data fetching vs Astro">
- React: มีสามรูปแบบแยกกัน — `getStaticProps` (build), `getServerSideProps` (per-request), `useEffect+fetch` (browser)
- Astro: **รูปแบบเดียว** — `await fetch()` ใน frontmatter ไม่มีการ fetch ฝั่ง browser โดย default
- React `useEffect+fetch` ทำงานใน browser ทุกครั้งที่โหลดหน้า; Astro frontmatter fetch ทำงาน **ศูนย์ครั้ง** ใน browser
- Astro content collections เพิ่ม **การจัดการ MD/MDX แบบ type-safe** โดยไม่ต้องใช้ library ของ third-party
</Diff>

export const quizIndex = [
  {
    q: "โค้ดดึงข้อมูลใน Astro component ทำงานที่ไหน?",
    options: [
      "ใน browser ผ่าน useEffect",
      "ใน frontmatter (`---`) ตอน build หรือบน server",
      "ในไฟล์ข้อมูลแยกต่างหาก",
      "ใน HTML template",
    ],
    answer: 1,
  },
  {
    q: "ฟีเจอร์ใดของ Astro ที่ให้การจัดการเนื้อหา MD/MDX แบบ type-safe?",
    options: [
      "Astro.props",
      "Content collections",
      "Astro.glob()",
      "import.meta.env",
    ],
    answer: 1,
  },
  {
    q: "โดย default Astro ส่ง JS สำหรับดึงข้อมูลไปที่ browser ไหม?",
    options: [
      "ไม่ — การดึงข้อมูลเกิดขึ้นตอน build/server เท่านั้น",
      "ใช่ — Astro hydrate fetch calls",
      "เฉพาะเมื่อใช้ `client:load`",
      "ขึ้นอยู่กับ adapter",
    ],
    answer: 0,
  },
];

<Quiz client:visible id="content/index" questions={quizIndex} />

<ProgressTracker client:visible id="content/index" />
