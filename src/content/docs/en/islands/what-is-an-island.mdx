---
title: What is an Island?
description: 'Partial hydration vs full-app hydration: why shipping a whole React runtime is expensive, and how islands cut the cost.'
---

import ReactAstro from '../../../../components/ReactAstro.tsx';
import Diff from '../../../../components/Diff.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

In a React or Next.js app the browser receives **one large JavaScript bundle**. React boots up, walks the entire component tree, and attaches event listeners everywhere — even on components that never do anything interactive. This is full-app hydration.

The Island Architecture, coined by Jason Miller and popularised by Astro, inverts the default: pages are inert HTML. Only the components that need interactivity are hydrated, and they hydrate **independently** — like islands in a sea of static markup.

## The hydration cost in React vs Astro

export const hydrationReact = `// Next.js page — the WHOLE tree hydrates.
// Even <Header> and <Footer> download JS.
export default function BlogPost({ post }) {
  return (
    <>
      <Header />         {/* JS downloaded + hydrated */}
      <article>{post.content}</article>  {/* JS downloaded + hydrated */}
      <LikeButton postId={post.id} />    {/* JS downloaded + hydrated */}
      <RelatedPosts posts={post.related} />{/* JS downloaded + hydrated */}
      <Footer />         {/* JS downloaded + hydrated */}
    </>
  );
}
// Total JS budget: EVERYTHING`;

export const hydrationAstro = `---
// Astro page — only <LikeButton> needs JS.
import Header from '../components/Header.astro';
import LikeButton from '../components/LikeButton.jsx';
import RelatedPosts from '../components/RelatedPosts.astro';
import Footer from '../components/Footer.astro';
const { post } = Astro.props;
---
<Header />                        <!-- 0 KB JS -->
<article>{post.content}</article>  <!-- 0 KB JS -->
<LikeButton client:visible postId={post.id} /> <!-- hydrated -->
<RelatedPosts posts={post.related} />  <!-- 0 KB JS -->
<Footer />                        <!-- 0 KB JS -->
{/* Total JS budget: ONE component */}`;

<ReactAstro client:visible react={hydrationReact} astro={hydrationAstro} />

On a typical content page — a blog post, a landing page, a docs site — the vast majority of components are purely presentational. They display data; they never respond to clicks. Hydrating them wastes CPU and delays Time to Interactive.

## The performance win

Astro measures this precisely. A page with a single `LikeButton` island only downloads the JavaScript for that button and its React runtime — nothing else. A Next.js equivalent ships React plus every component on the page.

Typical real-world results on content-heavy sites:

- **90%+ reduction** in JavaScript shipped
- **Faster Largest Contentful Paint** — the HTML arrives parsed, no hydration blocking rendering
- **Lower Time to Interactive** — only one small island needs to boot

## What "partial hydration" means

The pattern is called **partial hydration** because only a *part* of the page is hydrated. Each island:

1. Renders to HTML on the server/build (like any `.astro` component)
2. Ships its own JS bundle to the browser
3. Hydrates **independently** — islands do not share a React root

That last point matters: islands are isolated. There is no single React tree connecting them, which means no shared React context across islands by default. You will learn how to work with that in [When to Use an Island](/astro-for-react-developers/en/islands/when-to-island/).

export const islandConceptReact = `// React: ONE root, one reconciler, shared context
import ReactDOM from 'react-dom/client';
ReactDOM.createRoot(document.getElementById('root'))
  .render(<App />);
// Every component inside <App> is part of this tree.
// React.createContext() works across the whole tree.`;

export const islandConceptAstro = `---
// Astro: MULTIPLE roots, one per island
// Astro mounts each island independently:
//   ReactDOM.createRoot(islandEl).render(<Counter />)
//   ReactDOM.createRoot(likeEl).render(<LikeButton />)
// Each island has its own React root.
// React.createContext() does NOT cross island boundaries.
import Counter from '../components/Counter.jsx';
import LikeButton from '../components/LikeButton.jsx';
---
<Counter client:load />
<LikeButton client:visible postId={42} />`;

<ReactAstro client:visible react={islandConceptReact} astro={islandConceptAstro} />

<Diff title="Full hydration vs Partial hydration">
- React/Next.js: **one React root** at `document.getElementById('root')` — the whole app.
- Astro: **one React root per island** — each component hydrates in isolation.
- React Context works across an entire React app; in Astro it only works within a single island.
- Astro still uses React under the hood — your hooks, context, and state all work **inside** one island.
</Diff>

export const quizWhatIsIsland = [
  {
    q: "What is 'partial hydration'?",
    options: [
      "Hydrating only the components that need JavaScript interactivity",
      "Rendering only half the page on the server",
      "A React feature for lazy loading components",
      "Sending compressed JavaScript to the browser",
    ],
    answer: 0,
  },
  {
    q: "In Astro, how many React roots are created for a page with three React islands?",
    options: [
      "One shared root for all islands",
      "Zero — Astro does not use React roots",
      "Three — one per island",
      "Depends on whether the islands share state",
    ],
    answer: 2,
  },
  {
    q: "Why does full-app hydration increase Time to Interactive even on mostly-static pages?",
    options: [
      "The server takes longer to render HTML",
      "The browser must download and execute JS for every component, including purely presentational ones",
      "React requires a database connection to hydrate",
      "The browser caches React bundles, slowing the first load",
    ],
    answer: 1,
  },
  {
    q: "Does React Context work across two separate Astro islands on the same page?",
    options: [
      "Yes — Astro links islands together with a global context bridge",
      "No — each island is an independent React root; context does not cross island boundaries",
      "Only if both islands import the same context file",
      "Only with `client:only`",
    ],
    answer: 1,
  },
];

<Quiz client:visible id="islands/what-is-an-island" questions={quizWhatIsIsland} />

<ProgressTracker client:visible id="islands/what-is-an-island" />
